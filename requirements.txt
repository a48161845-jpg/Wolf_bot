aiogram>=3.7,<4
# Нужен только если используешь PostgreSQL (задана переменная окружения DATABASE_URL).
# Если работаешь на локальном файле SQLite (DATABASE_URL не задана) — можно не ставить.
psycopg2-binary>=2.9
